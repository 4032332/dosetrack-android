# Copy to secrets.properties (gitignored) and fill in.
# Reuses the existing DoseTrack Supabase project (the same one the iOS app uses).
# Not required to build the scaffold — the build falls back to these example values.
SUPABASE_URL=https://YOUR-PROJECT.supabase.co
SUPABASE_ANON_KEY=YOUR-ANON-KEY
